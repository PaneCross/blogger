<p id="notice"><%= notice %></p>

<h1>Authors</h1>

<table>
  <thead>
    <tr>
      <th><b>Username</b></th>
      <th class="m"><b>Email</b></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td><%= author.username %></td>
        <td class="n"><%= author.email %></td>
        <td class="n"><%= link_to 'Show', author %></td>
        <td class="n"><%= link_to 'Edit', edit_author_path(author) %></td>
        <td class="n"><%= link_to 'Delete', author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Author', new_author_path %>
